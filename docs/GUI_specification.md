Draft

Overall layout similar to
https://jsbien.github.io/typoglyphs/

Menu and version line

Several search windows, at first just two:
Mysql console or a simple query constructor (if not difficult)
Document area in the djview format (to be implemented later)

Below large scrolable image grid  and additional side panel

Menu item: 

sorting - now only default one
selection - only roots or the  full grid

Clicking on a grid images displayes more information about the shape
in the side panel. This includes in particular the document file name
and the page number.


The side panel should additionally contain the button for navigating
in the hierarchy tree and displaying the whole hierarchy in the main
panel (which must show then a back button).


